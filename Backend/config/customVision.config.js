export const grainTypeConfig = {
  endpoint: process.env.GRAIN_TYPE_ENDPOINT,
  projectId: process.env.GRAIN_TYPE_PROJECT_ID,
  modelName: process.env.GRAIN_TYPE_MODEL_NAME,
  key: process.env.GRAIN_TYPE_PREDICTION_KEY
};

export const wheatQualityConfig = {
  endpoint: process.env.WHEAT_QUALITY_ENDPOINT,
  projectId: process.env.WHEAT_QUALITY_PROJECT_ID,
  modelName: process.env.WHEAT_QUALITY_MODEL_NAME,
  key: process.env.WHEAT_QUALITY_PREDICTION_KEY
};
